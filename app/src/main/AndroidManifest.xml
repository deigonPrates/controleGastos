<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.deigon.concroledegastos2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Casa" />
        <activity android:name=".Receita" />
        <activity android:name=".Listar" />
        <activity android:name=".ListarGastos" />
        <activity android:name=".Config" />
        <activity android:name=".sobre" />
        <activity android:name=".define_mes"></activity>
    </application>

</manifest>